services:
  image-api:
    image: yuwancumian2009/randompicapi:latest
    container_name: image-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./images:/app/images
